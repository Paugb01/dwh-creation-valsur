# Environment Variables Template
# Copy this file to .env and update with your actual paths
# DO NOT commit .env to version control!

# Google Cloud Storage Service Account Key
GCS_APPLICATION_CREDENTIALS=path/to/your/gcs-service-account.json

# BigQuery Service Account Key  
BQ_APPLICATION_CREDENTIALS=path/to/your/bq-service-account.json

# Alternative: You can use the same service account for both services
# GCS_APPLICATION_CREDENTIALS=path/to/your/service-account.json
# BQ_APPLICATION_CREDENTIALS=path/to/your/service-account.json

# Instructions:
# 1. Download your service account key(s) from Google Cloud Console
# 2. Place them in a secure location (e.g., .keys/ directory)
# 3. Update the paths above to point to your actual key files
# 4. Ensure your service account has the following roles:
#    - Storage Admin (for GCS operations)
#    - BigQuery Data Editor (for BigQuery operations)
#    - BigQuery Job User (for BigQuery operations)
